import { Chip, Tab, Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

const StyledPageHeadingTypography = styled((props) => <Typography variant="h4" mb={4} {...props} />)(({ theme }) => ({

  }));

export default StyledPageHeadingTypography;