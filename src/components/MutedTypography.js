import { Chip, Tab, Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

const MutedTypography = styled((props) => <Typography pl={1} color={"#757575"} fontSize={"0.9rem"} {...props} />)(({ theme }) => ({

  }));

export default MutedTypography;